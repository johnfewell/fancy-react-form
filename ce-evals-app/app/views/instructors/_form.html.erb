<%= form_for @instructor, builder: PrimerFormBuilder do |f| %>
  <% if @instructor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instructor.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @instructor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br>
    <%= f.text_field :first_name %><br>
    <%= f.label :last_name %><br>
    <%= f.text_field :last_name %><br>
    <%= f.label :title %><br>
    <%= f.text_field :title %><br>
    <%= f.label :suffix %><br>
    <%= f.text_field :suffix %><br>
  </div>
</br>
  <div class="actions">
    <%= f.submit 'Update Profile', :class => 'btn btn-primary' %>
  </div>
<% end %>
